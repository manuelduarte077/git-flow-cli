La fórmula Homebrew vive en la raíz del repo: [`Formula/git-bn-cli.rb`](../../Formula/git-bn-cli.rb).

Homebrew 5+ no admite `brew install https://raw.githubusercontent.com/.../formula.rb` ni `brew install ./ruta/local.rb` fuera de un tap. Usa `brew tap` + `brew install` (ver README principal).
